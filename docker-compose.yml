version: '3.8'
services:
  # 1. SERVIÇO DE BANCO DE DADOS
  postgres-paylure:
    image: postgres:15-alpine
    container_name: postgres-paylure
    restart: unless-stopped
    ports:
      # Expor na porta 5433 da sua VPS
      - "5433:5432" 
    environment:
      POSTGRES_USER: paylure_user
      POSTGRES_PASSWORD: paylure_password
      POSTGRES_DB: paylure_db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    # 🚨 BLOCO 'networks' REMOVIDO
    
  # 2. SERVIÇO DO BACKEND (NESTJS)
  api: 
    image: node:18-alpine
    container_name: paylure-api
    restart: unless-stopped
    depends_on:
      - postgres-paylure
    environment:
      # Conexão direta usando o nome do serviço
      DATABASE_URL: postgres://paylure_user:paylure_password@postgres-paylure:5432/paylure_db?schema=public
      JWT_SECRET: sua_chave_secreta_aqui 
    ports:
      - "3000:3000"
    # 🚨 BLOCO 'networks' REMOVIDO
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    working_dir: /usr/src/app
    
    command: sh -c "npm install && npm run start:dev"

# Volumes para persistência de dados
volumes:
  postgres-data:
    driver: local

# 🚨 BLOCO DE REDE REMOVIDO TOTALMENTE
# (Não deve haver a seção 'networks:' no final)